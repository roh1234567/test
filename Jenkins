pipeline {
    agent { 
        docker { 
            image "python:3.8"
            args '--user 0:0'
        } 

    }
    stages {
        stage('Run schemachange') {
            steps {
                sh "pip install schemachange --upgrade"
                sh "schemachange -f migrations -a ${https://ceudjkl-ib64558.snowflakecomputing.com} -u ${ry} -r ${accountadmin} -w ${compute_wh} -d ${jenkins} -c ${jenkins}.SCHEMACHANGE.CHANGE_HISTORY --create-change-history-table"
            }
        }
    }
}
