pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run Test') {
            steps {
                echo 'Running test.groovy'
                sh 'groovy test.groovy'
            }
        }
    }
}
